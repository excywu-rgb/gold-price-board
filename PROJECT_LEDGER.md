# 漂漂猪港口交易所：项目说明书与开发台账

> 文档版本：v1.0  
> 状态基线：2026-08-06 15:50（Asia/Shanghai）  
> 适用仓库：`excywu-rgb/gold-price-board` / `main`  
> 用途：产品、数据、设计、运维和并行开发的共同真源。任何新对话先读本文件，再确定工作包。

---

## 1. 项目定位

本项目当前是一套面向冒险岛怀旧服国服「漂漂猪」服务器的游戏币市场观察工具。它从 DD373 公开挂牌页按小时采集游戏币价格和最大可买量，保留历史快照，生成游戏化可视看板，并通过 GitHub Pages 对外发布。

当前已正式交付的业务范围只有：

- 漂漂猪服务器游戏币挂牌价格；
- 全部分页挂牌数量与货量；
- 最低价、主流价格带、加权均价和扫货预算；
- 金价历史趋势与总货量趋势；
- 当前挂牌明细及价格带筛选。

DD373 的账号、装备、道具、代练或其他非金价数据尚未进入正式范围。它们可以成为下一阶段，但必须先作为独立数据域开发，不能直接扩张现有金价数据契约。

### 1.1 产品原则

1. **数据可信优先于准点**：抓取不完整时宁可保留上一轮，也不发布伪全量数据。
2. **历史只增不丢**：允许同小时替换，不允许其他既有小时记录消失。
3. **来源状态可见**：全量/非全量、页数、源站报告数和抓取时间必须可追溯。
4. **异常不等于错误**：超大挂牌可能真实存在；展示时标注其贡献，不能静默删除。
5. **好玩但不失真**：视觉采用冒险岛式天空、草地、港口、金币和任务板语言；装饰不能掩盖口径和数据质量。
6. **生产与实验隔离**：新数据域、影子抓取和视觉实验不能改写正式金价历史。
7. **自动化可降级**：云端主刷新失败时保留旧值，本机仅在达到陈旧阈值后兜底。

### 1.2 当前不做的事情

- 不把挂牌价描述为真实成交价；
- 不把 `qty_max` 合计描述为平台真实库存；
- 不保证 DD373 页面结构永久稳定；
- 不在源站分页不完整时发布部分数据；
- 不依赖 Mac 永久在线作为唯一刷新方式；
- 不把早期只抓首页的 20 单样本伪装成全量历史。

---

## 2. 真源、环境与发布地址

| 角色 | 位置 | 规则 |
|---|---|---|
| 正式本地仓库 | `/Users/wuchaoyin/gold-price-board` | 唯一生产工作副本；launchd 使用此目录 |
| 旧工作副本 | `/Users/wuchaoyin/Documents/kimi/Workspaces/常规工作产出/冒险岛金价工具/gold-price-board` | 仅兼容旧工作流；不得作为定时任务目录；合入后再同步 |
| GitHub 仓库 | `https://github.com/excywu-rgb/gold-price-board` | 远端真源，默认分支 `main` |
| 线上页面 | `https://excywu-rgb.github.io/gold-price-board/` | GitHub Pages，从 `docs/` 发布 |
| 正式历史 | `docs/data/gold_history.json` | 生产数据真源，不接受实验写入 |
| 页面生成器 | `scripts/generate_html.py` | 页面源码真源 |
| 页面生成物 | `docs/index.html` | 不单独手改 |
| 本机任务 | `~/Library/LaunchAgents/com.goldpriceboard.refresh.plist` | 每小时 `:47`，带 `--fallback` |
| 本机日志 | `logs/refresh.log` / `logs/refresh.err.log` | 不提交 Git；只作本机运行证据 |

注意：旧资料中曾写作 `com.goldprice-board.refresh.plist`，当前真实文件名和 launchd Label 均为 `com.goldpriceboard.refresh`，以本表为准。

---

## 3. 生产架构

### 3.1 主链路

```text
Cloudflare Cron (:23)
  → POST GitHub workflow_dispatch
  → .github/workflows/refresh.yml
  → scripts/fetch_gold.py
  → docs/data/gold_history.json（原子写入）
  → scripts/generate_html.py
  → docs/index.html
  → scripts/commit_state.py
  → main
  → GitHub Pages
```

- Cloudflare Worker 只负责触发，不读取和写入正式数据。
- 触发标题必须含 `cloudflare-cron:23 * * * *`，便于区分自然执行和人工验证。
- GitHub workflow 使用 `concurrency: gold-price-production-refresh` 串行化，避免两个生产刷新并发写入。
- 正式 workflow 只接受 `workflow_dispatch`；当前不使用 GitHub 原生 `schedule`。

### 3.2 本机兜底

```text
launchd (:47)
  → scripts/refresh_local.py --fallback
  → fetch + pull --ff-only
  → 检查 origin/main 最新快照年龄
  → < 75 分钟：跳过
  → ≥ 75 分钟：执行抓取、生成、提交和推送
```

兜底的目标不是补齐每一个缺失小时，而是避免线上数据长期陈旧。Mac 关机、休眠或未登录时，本机兜底不具备持续可用性。

### 3.3 已完成的影子测试

`.github/workflows/shadow-12h-test.yml` 曾对真实 DD373 数据源执行 12 小时隔离测试，证据写入 `shadow-test-results` 分支，不触碰正式历史。该 workflow 当前 GitHub 状态为 `disabled_manually`，不得重新启用，除非新测试方案明确要求。

---

## 4. 金价数据契约 v1

正式文件顶层结构：

```json
{
  "history": [
    { "time": "YYYY-MM-DD HH:MM:SS", "...": "每小时快照" }
  ]
}
```

### 4.1 快照字段

| 字段 | 类型 | 含义 | 口径/约束 |
|---|---|---|---|
| `time` | string | 源站响应时间换算后的北京时间 | 小时内唯一；同小时可替换 |
| `lowest` | number | 当前最低挂牌单价 | 元/万金，不是成交价 |
| `main_low` / `main_high` | number | 主流价格带边界 | 排序后中间 50% 挂牌价格范围 |
| `main_count` | integer | 主流价格带单数 | 价格落在边界内的挂牌数 |
| `weighted_avg` | number | 挂牌加权均价 | 按每单 `qty_max` 加权 |
| `total_qty` | integer | 港口总货量 | 所有挂牌 `qty_max` 之和，单位万金 |
| `total_amount` | number | 一键扫货预算 | `Σ(price_per_wan × qty_max)`，单位元 |
| `n_listings` | integer | 实际解析挂牌数 | 去重后的全分页结果 |
| `source_pages` | integer | 抓取页数 | 当前分页遍历证据 |
| `source_reported_total` | integer/null | 源站宣称挂牌数 | 用于覆盖率校验 |
| `coverage_complete` | boolean | 是否达到全量校验 | 不为 `true` 时不得当作全量样本 |
| `items` | array | 本轮挂牌明细 | 见下表 |

### 4.2 挂牌字段

| 字段 | 类型 | 含义 |
|---|---|---|
| `price_per_wan` | number | 元/万金 |
| `qty_min` | integer | 单次最小可买量，单位万金 |
| `qty_max` | integer | 最大可买量，单位万金 |
| `shop_number` | string，可选 | 源站挂牌标识，用于跨页去重 |

### 4.3 数据质量规则

- 第一页解析分页链接，再抓取全部数字页。
- 优先按 `shop_number` 去重；缺少时以价格和数量组合作备用键。
- 页面报告数大于实际解析数时立即失败，正式文件保持不变。
- 解析结果为空时立即失败。
- 旧 JSON 结构无效、时间字段缺失或时间戳重复时拒绝覆盖。
- 写入采用同目录临时文件、`fsync` 和 `os.replace` 原子替换。
- 当前历史中最早 4 条记录来自旧版首页采样，没有全量覆盖证据；看板以灰色虚线单独显示。

---

## 5. 页面信息架构与设计原则

页面由 `scripts/generate_html.py` 生成，当前模块顺序为：

1. 顶部品牌与自动刷新状态；
2. 当前最低挂牌主视觉；
3. 行情快照和全量覆盖状态；
4. 加权均价、挂牌数、总货量、扫货预算四项指标；
5. **金价航线**：最低挂牌与加权均价趋势；
6. **摊位分布**：低价/主流/高价区；
7. **港口货量潮汐**：总货量与挂牌数趋势、超大单提示、非全量历史标记；
8. **港口摊位清单**：当前挂牌明细和价格带筛选。

视觉延续冒险岛式轻松游戏感：天空蓝、草地绿、金币黄、枫叶橙、奶油色卡片、圆角和任务板文案。新增模块应复用 CSS 变量和组件语言，不引入写实金融终端风格。

数据可视化必须满足：

- 默认展示 24 小时，并支持 7 天、30 天；
- 价格轴和数量轴明确单位；
- 不完整样本必须与完整样本视觉区分；
- 超大单导致的总量突变要给出最大单及占比解释；
- 移动端不得横向溢出；
- Chart.js 加载失败时提供文本降级，不让空白画布伪装成功。

---

## 6. 文件保护等级与所有权

### P0：生产核心，默认禁止并行修改

| 文件/资源 | 风险 | 允许变更条件 |
|---|---|---|
| `docs/data/gold_history.json` | 历史丢失、线上错误数据 | 仅正式刷新流程；开发者只读 |
| `scripts/fetch_gold.py` | 抓取口径和写入安全变化 | 独立修复工作包 + 数据契约测试 + 真实源影子验证 |
| `.github/workflows/refresh.yml` | 正式刷新中断或并发覆盖 | 运维工作包 + workflow 审核 + 不手动冒充自然执行 |
| `cloudflare-scheduler/*` | 云端定时触发中断 | 调度专项工作包；密钥不得入库 |
| launchd plist | 本机兜底中断或抢跑 | 用户明确授权 + `launchctl` 实测 |

### P1：共享生成链路，必须指定单一集成人

| 文件 | 规则 |
|---|---|
| `scripts/generate_html.py` | 页面源码；多个视觉分支不得直接同时合并 |
| `docs/index.html` | 只能由生成器更新并与其同一提交 |
| `scripts/refresh_local.py` | 保持 `pull --ff-only`、75 分钟阈值和 `--fallback` 语义 |
| `scripts/commit_state.py` | 只提交 `docs/`，不得扩大到整个仓库 |

### P2：可独立扩展

- 新的数据域脚本、模型和测试；
- `research/` 下的说明、样本定义和非生产实验；
- 不被正式页面引用的新数据文件；
- 独立的分析报告和原型页面。

P2 一旦接入 `docs/`、正式 workflow 或共享生成器，即自动升级为 P1/P0，必须走集成 Gate。

---

## 7. 多对话并行开发协议

### 7.1 工作包登记

每个新对话先在本文件末尾“工作包台账”登记，至少写清：

- 工作包 ID 和目标；
- 基线 commit；
- 分支名；
- 允许修改文件；
- 明确禁止修改文件；
- 输入/输出数据契约；
- 验收标准；
- 当前状态：`待开始 / 进行中 / 待集成 / 已完成 / 已阻塞 / 已放弃`。

### 7.2 分支和同步

```bash
git fetch origin
git switch -c feat/<domain>-<topic> origin/main
```

- 一个工作包一个分支，不让多个对话共同操作同一分支。
- 不在工作包中执行 `git reset --hard`、覆盖正式 JSON 或清理他人的未跟踪文件。
- 合入前先获取最新 `origin/main`；若自动刷新产生新数据提交，优先保留远端正式数据，再重新生成页面。
- 自动刷新会频繁推进 `main`。涉及 `docs/index.html` 的功能提交，应在集成前重新运行生成器，不能接受旧页面覆盖新数据。

### 7.3 文件冲突分配

| 并行场景 | 决策 |
|---|---|
| 两个数据域各自新增脚本与测试 | 可并行 |
| 两个工作包都改 `generate_html.py` | 不直接并行合并；指定一个页面集成包 |
| 新抓取器与金价抓取器 | 目录、schema、输出文件完全隔离 |
| 新模块需要正式定时 | 先手动/影子运行，再单独做调度集成包 |
| 自动数据提交与功能分支冲突 | 以最新正式 JSON 为准，重跑生成器 |

### 7.4 集成 Gate

**Gate A：静态安全**

- 文件范围符合工作包；无密钥、日志、缓存或大体积原始页面；
- `git diff --check` 通过；
- 没有无关生产配置变化。

**Gate B：自动测试**

```bash
python3 -m unittest discover -s tests -v
python3 -m py_compile scripts/*.py
```

**Gate C：数据保护**

- 开发前后正式 JSON 哈希一致，除非该工作包就是经过授权的数据刷新；
- 历史时间戳唯一；
- 既有记录未丢；
- 不完整抓取不会覆盖旧数据。

**Gate D：页面生成与视觉**

```bash
python3 scripts/generate_html.py docs/data/gold_history.json docs/index.html
```

- 桌面、390px 手机宽度实测；
- 图表切换和列表筛选可用；
- 浏览器控制台无新增错误；
- 新文案不把挂牌数据写成成交数据。

**Gate E：生产集成**

- 最新 `origin/main` 已纳入；
- workflow 调度没有被误改；
- Pages HTTP 200；
- 带缓存破坏参数的线上 JSON 与 `origin/main` 一致；
- 需要变更自动化时，先影子验证，再显式启用。

---

## 8. DD373 非金价数据的推荐扩展方案

下一阶段不要继续把所有逻辑塞进 `fetch_gold.py` 和 `gold_history.json`。建议建立独立的数据平台分层：

```text
scripts/dd373/
  common.py               # 请求、限速、时间、通用解析工具
  <domain>_fetch.py       # 账号/装备/道具等领域抓取器
  <domain>_normalize.py   # 原始页面 → 稳定 schema
  <domain>_metrics.py     # 统计与分析

research/dd373/
  source-map.md           # 页面、筛选条件、分页和字段证据
  schemas/                # 草案 schema 与样本说明

tests/dd373/
  fixtures/               # 脱敏的小样本 HTML/JSON
  test_<domain>_*.py

data/dd373-lab/            # 非公开实验输出，不接入 Pages
docs/data/<domain>/        # 通过发布 Gate 后才允许出现
```

目录名称是目标结构建议，尚未创建；具体领域确认后再落地。

### 8.1 新数据域必须独立回答的问题

1. 页面和筛选条件唯一标识什么商品或服务？
2. 源站报告总数、分页规则、动态加载和去重键是什么？
3. 价格单位、数量单位、区服、职业、等级、属性等字段如何解释？
4. 挂牌更新、下架和重复发布如何识别？
5. 哪些字段是源站原值，哪些是解析值，哪些是推导指标？
6. 是否涉及登录、验证码、个人信息、卖家信息或平台条款风险？
7. 采样频率是否真的需要每小时？是否应按领域降低频率？

### 8.2 推荐开发阶段

| 阶段 | 输出 | 禁止事项 | 通过标准 |
|---|---|---|---|
| D0 来源侦察 | 页面地图、字段字典、样本证据 | 不接生产定时 | 明确分页、总数和唯一键 |
| D1 离线解析 | fixture + parser + 单测 | 不访问正式 JSON | 样本可重复解析，字段有口径 |
| D2 手动真实抓取 | 独立实验结果 | 不写 `docs/data` | 多轮结果完整、去重稳定 |
| D3 影子运行 | 独立证据分支或 artifact | 不改正式页面 | 触发率、抓取成功率、覆盖率可量化 |
| D4 分析建模 | 指标定义和异常规则 | 不先做漂亮页面 | 指标能解释业务问题 |
| D5 页面集成 | 新模块/新页面 | 不覆盖金价生成物 | 移动端、降级和口径验收通过 |
| D6 正式调度 | 独立 concurrency 和数据文件 | 不复用金价写入组 | 先灰度，再启用；失败不影响金价 |

### 8.3 隔离要求

- 每个领域使用独立 schema 名称和版本，例如 `dd373-account-listings/v1`。
- 每个领域使用独立输出文件和时间序列，不在 `gold_history.json` 增加异构字段。
- 每个领域使用独立 workflow concurrency group；一个领域失败不能取消另一个领域。
- 通用网络工具只能抽取无业务语义的能力；金价特有分页、统计和写入规则不要过早“通用化”。
- 原始 HTML 默认不提交；测试 fixture 必须小型、脱敏、注明采样时间和用途。
- 新页面优先独立原型。确认信息架构后再由页面集成包接入主看板。

---

## 9. 当前运行状态与已知风险

### 9.1 已确认状态（截至基线时间）

| 项目 | 状态 | 证据 |
|---|---|---|
| 线上看板 | 正常发布 | GitHub Pages 已部署，公开地址可访问 |
| 正式历史 | 51 条，时间戳唯一 | 最新 `2026-08-06 14:47:06` |
| 最新完整性 | 完整 | 4 页 / 源站 75 单 / 实际 75 单 / `coverage_complete=true` |
| 早期非全量 | 4 条 | 旧版未记录分页覆盖率；页面单独标识 |
| Cloudflare 触发 | 仍按 `:23` 自然触发 | Actions 标题含 `cloudflare-cron:23 * * * *` |
| 本机兜底 | 已加载，退出状态 0 | `com.goldpriceboard.refresh`，`:47`，含 `--fallback` |
| 影子 workflow | 已停用 | GitHub API 状态 `disabled_manually` |

### 9.2 当前异常和技术债

1. **最近两轮云端抓取失败**：2026-08-06 14:23、15:23 的 Cloudflare 自然触发均成功到达 GitHub，但失败于“抓取 DD373 金价”步骤。14:47 本机兜底写入了完整的 75 单快照；15:47 因最新数据年龄未达到 75 分钟而按规则跳过。影响是小时采样可能缺口，不是历史覆盖或脏数据。
2. **本机日志缺少每行时间戳**：只能借助文件修改时间和提交时间还原轮次；建议后续为 stdout/stderr 增加结构化时间和触发来源。
3. **分页器解析依赖 HTML 结构**：DD373 改版或列表瞬态不一致会触发失败关闭。安全性正确，但需要重试、页面证据留存和错误分类。
4. **兜底阈值不补齐每小时缺口**：75 分钟规则面向可用性，不保证连续时间序列。如业务要求“每小时必有一个点”，需另立补采/缺失标记方案，不能简单降低阈值造成云端和本机争抢。
5. **两个本地 clone 容易漂移**：正式开发应以 `/Users/wuchaoyin/gold-price-board` 为准；旧副本只在合入后快进同步。
6. **生成器为单文件**：HTML、CSS、JS 和数据注入集中在 `generate_html.py`，页面模块继续增长后需要受控拆分，但拆分本身应是独立工作包。
7. **监控仍以人工查看 Actions/日志为主**：尚无失败告警、连续失败计数和 Pages 陈旧状态提示。

---

## 10. 变更台账

| 日期 | 版本/提交 | 变更 | 影响范围 | 结论 |
|---|---|---|---|---|
| 2026-08-04 | 早期版本 | 从首页 20 单扩展为分页全量抓取 | 数据契约、历史口径 | 前 4 条保留为非全量历史，不删除 |
| 2026-08-04—05 | 影子测试 | 真实数据源 12 小时隔离验证 | `shadow-test-results`、影子 workflow | 不改正式数据；workflow 已停用 |
| 2026-08-06 | `09a36f2` 等 | Cloudflare 外部定时触发 + 本机延迟兜底 | 正式调度 | Cloudflare `:23` 主触发，launchd `:47` 兜底 |
| 2026-08-06 | 三轮验收 | 09:23、10:23、11:23 自然运行 | Actions、历史、Pages | 3/3 触发、抓取、完整性成功；历史无丢失 |
| 2026-08-06 | `96089a7` | 新增“港口货量潮汐” | 生成器、页面、测试 | 总货量/挂牌数双轴、超大单和非全量标记上线 |
| 2026-08-06 | 当前文档 | 建立说明书、保护规则和并行开发台账 | 协作治理 | 后续工作包以本文件为共同真源 |

---

## 11. 工作包台账

> 新工作开始时追加一行；完成后补充提交、验证和遗留项。不要改写既有历史结论。

| ID | 工作包 | 分支/基线 | 允许修改 | 禁止修改 | 状态 | 验收/交付 |
|---|---|---|---|---|---|---|
| OPS-001 | 外部定时正式方案 | 已合入 `main` | 调度、workflow、兜底脚本 | 非必要页面功能 | 已完成 | 三轮自然执行验收；后续出现源站抓取失败，见风险 1 |
| UI-001 | 看板视觉重构 | 已合入 `main` | `generate_html.py`、生成页面 | 正式历史口径 | 已完成 | 冒险岛港口任务板视觉上线 |
| UI-002 | 港口货量潮汐 | `96089a7` | 生成器、生成页面、相关测试 | 正式历史 | 已完成 | 桌面/手机、范围切换、异常大单、非全量标识通过 |
| DOC-001 | 项目说明书与并行治理 | 见提交历史 | `README.md`、`AGENTS.md`、本文件 | 正式数据与调度 | 已完成 | 文档与真实代码、运行状态一致 |
| DATA-002 | DD373 非金价来源侦察 | 待建立 | 建议仅 `research/dd373/` 和独立测试 | 金价 JSON、正式 workflow、主页面 | 待开始 | 先完成 D0 来源地图和字段字典 |

### 工作包启动模板

```markdown
工作包 ID：
目标：
业务问题：
基线 commit：
分支：
允许修改文件：
禁止修改文件：
输入契约：
输出契约：
验收标准：
风险与回滚：
状态：待开始
```

### 对新对话的推荐开场指令

```text
请先完整阅读仓库根目录 README.md、AGENTS.md 和 PROJECT_LEDGER.md。
本次只处理工作包 <ID>。先核对当前 origin/main 和台账状态，列出允许/禁止修改文件，
不要改写 docs/data/gold_history.json，不要调整正式调度；发现与台账不一致时先报告证据。
完成后更新工作包状态、验证证据和遗留风险。
```

---

## 12. 文档维护规则

- 代码和实时运行证据优先于本文件；发现漂移时，用独立文档工作包校准，不静默改结论。
- “已确认”必须有源码、Git、Actions、launchd、HTTP 或数据文件证据。
- 规划中的目录、指标和功能必须标为建议或待开始，不能写成现状。
- 每次正式功能合入至少更新：变更台账、工作包状态、文件保护范围和已知风险。
- 自动数据刷新提交无需逐小时登记；只有架构、口径、功能、事故和治理变化进入台账。
